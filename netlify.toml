[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_BASE_URL = "/api"
  PHAKATHI_STORAGE = "netlify-blobs"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."scheduled-notifications"]
  schedule = "0 7,11,14 * * *"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
